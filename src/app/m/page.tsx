import { MaatHomePage } from './MaatHomePage';

export const metadata = {
  title: 'MA\'AT - Proof of Experience | Maat',
  description: 'AI-powered restaurant verification. Verify who is speaking, not what they say.',
};

export default function MaatPage() {
  return <MaatHomePage />;
}
